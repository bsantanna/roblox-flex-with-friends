*.lock
# Keep the Wally lockfile tracked for reproducible installs.
!wally.lock

# Wally-managed dependency outputs (regenerated by `wally install`)
/Packages
/ServerPackages
/DevPackages

# Rojo build artifacts and any local Studio-saved place
# (source of truth is src/ + default.project.json, not a binary .rbxl)
*.rbxl
build.rbxlx

# CI-generated analysis inputs
sourcemap.json
globalTypes.d.luau

# Editor
.vscode/
